## Handoff -- main -- 2026-06-24

**Session duration**: focused (View 2 pipeline wiring + docs)
**Workstream**: main

### What was built
View 2 (Conviction Tracker) is now COMPLETE and wired end-to-end:
- **Builder/writer (Prompts 1–2):** `build_conviction_adds_view` in `src/celebpm/views.py` +
  `write_conviction_adds_view` in `src/celebpm/view_io.py`. One row per ACTIVE_ADD event with
  forward returns, add-type classification (adding-to-winners vs averaging-down), and
  follow-through stats. Writes a HEADER-ONLY CSV for the empty case.
- **Standalone runner:** `python -m celebpm.build_views <CIK> --data-root data` (`build_views.py`)
  rebuilds View 1 + View 2 OFFLINE from the existing per-investor JSON (no network).
- **This prompt — pipeline wiring (`src/celebpm/pipeline.py`):** `run_pipeline` now builds + writes
  View 2 from the SAME in-memory `config`/`resolve.positions`/`changes`/`returns` (no re-read, no
  extra network), immediately after the View-1 write. `PipelineResult` gained four fields:
  `n_conviction_adds`, `conviction_csv_path`, `conviction_summary_path`, `conviction_summary`.
  `main()` now prints the conviction count + CSV + summary paths. Module docstring updated to name
  View 1 + View 2.
- **Tests (`tests/test_pipeline.py`):** added View-2 assertions to the three integration tests —
  existence of both artifacts, CSV columns == `constants.CONVICTION_ADDS_COLUMNS`, result path
  fields equal the on-disk locations, `n_conviction_adds == conviction_summary.total_adds` (wiring
  guard), on-disk `total_adds` match, empty-state (0 rows + total_adds == 0), and bad-filing
  (both artifacts still produced).
- **Outputs:** `data/<slug>/views/conviction_adds.csv` + `conviction_adds_summary.json`.

**Verification (run AFTER this prompt's edits):** `mypy --strict` clean; `pytest 567 passed`.
Live pipeline smoke was SKIPPED to avoid network; instead the offline
`python -m celebpm.build_views 0002045724 --data-root data` was run and exited 0, confirming the
View-2 wiring shape (Situational Awareness → 69 new ideas, 16 conviction adds).

### Decisions made
View-2 deviations SD-V2-1…SD-V2-8 are transcribed durably in
`docs/implementation_notes/view2_conviction_tracker_implementation_notes.md`.
- **SD-V2-1 (NEEDS OPERATOR CONFIRMATION):** `still_held` is computed per security-CHAIN (True iff
  the (cusip, security_type) chain's last entry is at the dataset's `latest_period` and non-EXIT) —
  NOT per add-cycle. 2 of 3 QA reviewers preferred per-cycle. A test locks per-chain; flipping is a
  one-function change.
- The rest are mechanical: SD-V2-2 (`is_underlying_price` column omitted), SD-V2-3 (sort tiebreak
  cusip/security_type beyond the spec's two keys), SD-V2-4 (`quarters_held_before_add` counts
  observed not calendar quarters), SD-V2-5 (next-quarter = next chain entry), SD-V2-6
  (held-before-dataset = first entry of current cycle), SD-V2-7 (`add_type` boundary: prior
  return == 0.0 → AVERAGING_DOWN), SD-V2-8 (`return_rec` test factory gains `price_on_filing`).

### Current state
Phase 1 intact; Phase 2 View 2 COMPLETE and wired. All cross-cutting invariants unchanged: CUSIP
join key, equity/options separation, filing-date anchor. View 2 reuses the same in-memory inputs as
View 1 (no new network calls, no new pipeline structure beyond build+write).

### Known issues
Carried forward and still operator-open:
- **SD-4** — View-1 summary is a sibling `new_ideas_summary.json` (vs CSV footer rows).
- **SD-2** — `value` stored as-reported (units cancel in view math).
- **SD-V2-1** — `still_held` per-chain vs per-cycle (NEW; pending confirmation).
Plus prior data-side caveats: foreign CUSIPs unresolved on OpenFIGI no-key tier; EODHD F6 rate
limits unverified beyond the probe.

### Next step
Phase 2 continues — **View 3 (Exit Signals)** then **View 4 (Survivors)**: sibling builders in
`views.py` + writers in `view_io.py` over the existing `changes`/`returns`/`positions` JSON, using
the same build+write wiring pattern just added to `run_pipeline`. No pipeline structural change
beyond that pattern.

### Parallel work available
Views 3 & 4 are mutually independent builders over `changes`/`returns`/`positions`; curating
`data/symbol_overrides.json` for foreign-CUSIP symbol overrides.

### Key files
`src/celebpm/pipeline.py`, `src/celebpm/views.py`, `src/celebpm/view_io.py`,
`src/celebpm/build_views.py`, `src/celebpm/constants.py` (CONVICTION_* block),
`tests/test_pipeline.py`, `docs/specs/view2_conviction_tracker_spec.md`,
`docs/implementation_notes/view2_conviction_tracker_implementation_notes.md`.

### Context to load
`workstreams/main.md`, `docs/specs/view2_conviction_tracker_spec.md`,
`docs/implementation_notes/13f_analysis_pipeline_implementation_notes.md`,
`docs/implementation_notes/view2_conviction_tracker_implementation_notes.md`.
