## Handoff -- main -- 2026-06-12

**Session duration**: ~1 working day (spanned 2026-06-11 → 06-12 with an overnight pause; ~6.5h of active build time across 6 prompts)
**Workstream**: main

### What was built
Full Phase-1 13F analysis pipeline via `/multi-prompt-build` (6 prompts, each plan → 3-model QA → triage → execute → verify). New `src/celebpm/` modules:
- `constants.py` — all URLs/limits/thresholds/paths/patterns/column names (no hardcoded values anywhere else).
- `errors.py` — `CelebPMError` + `ConfigError`/`EdgarError`/`DiscoveryError`/`OpenFigiError`/`EodhdError`.
- `config_loader.py` — `load_investor(cik)` / `load_all_investors()` from `config/investors.json`.
- `ratelimit.py` — `TokenBucket` (capacity-1, injectable clock) + `parse_retry_after`.
- `edgar_client.py` — rate-limited EDGAR `HttpClient`; `discovery.py` — `discover_filings`/`latest_filing_per_period`.
- `parser.py` — locate/fetch info-table XML, `parse_positions_from_xml`, `refine_amendment_type`.
- `openfigi_client.py` + `cusip_map.py` — CUSIP→ticker resolution (+ global cusip cache, ambiguity flagging).
- `eodhd_client.py` + `price_types.py` + `price_cache.py` + `symbol_map.py` — EODHD prices (adjusted close, global per-symbol cache, hard history floor).
- `diff.py` — `compute_changes` (QoQ classification NEW/EXIT/ACTIVE_ADD/ACTIVE_TRIM/DRIFT_UP/DRIFT_DOWN/HOLD + split flag).
- `returns.py` — filing-date-anchored returns + entry-range + multi-quarter cumulative + SPY benchmark.
- `views.py` + `view_io.py` — View 1 (New Ideas Feed) builder + CSV/summary writer (pandas confined here).
- `pipeline.py` — thin end-to-end orchestrator + `python -m celebpm.pipeline <CIK>` CLI.
- `models.py` — all frozen `kw_only` records: FilingRecord, PositionRecord, CusipMapEntry, PositionChange, ReturnRecord, ChangeType.
- `storage.py` — atomic, path-safe flat-JSON I/O for all artifacts + public `safe_data_path`.
- `tests/` — 526 unit tests (mocked clients); `tests_live/` — 4 manual live-API smoke tests.
- Config/scaffold: `pyproject.toml` (mypy strict, pandas override, deps), `config/investors.json`, `.gitignore`.
- Docs: `docs/implementation_notes/13f_analysis_pipeline_implementation_notes.md` (full per-prompt decision log); workstream + decisions updated; per-prompt handoffs `handoffs/main-2026-06-1{1,2}-prompt{1..6}.md`.

### Decisions made
See `workstreams/main-decisions.md` (2026-06-12) for the full list with rationale. Headlines: EDGAR JSON-only (no HTML, SD-1); CUSIP-as-join-key / ticker-display-only; strict equity/options separation (dual weights; options→underlying directional signal); filing-date anchor + first-quarter-baseline; classification on `weight_pct_reported` with exact spec operators + split→HOLD; **SD-2 value units = accept-as-reported (your call)** + `value_thousands`→`value_reported` rename (SD-3); EODHD adjusted close + global cache + cumulative-to-EXIT + delisting carry-forward; SPY on every ReturnRecord + View-1 excess columns; View-1 summary as sibling JSON (SD-4); thin orchestrator with per-filing skip-and-warn (`timeline_degraded`).

### Current state
**Works, fully:** end-to-end pipeline (`mypy --strict` clean across 47 files; `pytest` 526 passing). Ran live for Situational Awareness (CIK 0002045724): 6 filings → 126 positions → 153 changes → 153 returns → 69 new ideas; `data/situational_awareness/views/new_ideas.csv` + `new_ideas_summary.json` produced (80% win rate, avg winner +37% / loser −12%). EODHD client live-validated (F1–F5 + 404/empty + split-adjustment). Nothing is half-built within Phase 1.

### Known issues
- **SD-4 unconfirmed**: View-1 summary stats are in a sibling `new_ideas_summary.json`, not CSV footer rows (spec said "at the bottom"). Needs operator confirmation.
- **Foreign CUSIPs unresolved**: OpenFIGI's no-key tier returns "No identifier found" for foreign issuers (G/N/M/Q-prefixed CUSIPs) → `ticker=None` → included in View 1 as unpriced (NA returns). Expected; a manual `symbol_overrides.json` or an OpenFIGI key would resolve more.
- **One ambiguous ticker** observed live (CUSIP 09173B107 → chose `1B2`); flagged via the ambiguity mechanism, display-only.
- **Atreides not yet run live** (26 quarters) — would be the first heavy EODHD/OpenFIGI-rate-limit exercise; conservative no-key limits (OpenFIGI 25/min, EODHD ~60/min) are guesses, tunable in `constants.py`.
- **F6 (EODHD rate limits) unverified** beyond the single probe; **empty-quarter/100%-liquidation** invisibility documented (would need FilingRecord periods passed into `compute_changes`).

### Next step
Confirm SD-4 (sibling summary JSON acceptable?), then either (a) run the pipeline for Atreides — `.venv/bin/python -m celebpm.pipeline 0001777813 --data-root data` (with `.env` sourced) — to validate at scale, or (b) start Phase 2 View 2 (Conviction Tracker): add `build_conviction_view` in `src/celebpm/views.py` consuming `changes.json` (filter `ACTIVE_ADD`, sort by `weight_delta_bps`) + the matching `returns.json`, with a writer in `view_io.py`. No pipeline/model changes needed.

### Parallel work available
- Views 2–4 (Conviction/Exit/Survivors) are independent of each other and of any pipeline change — each is a builder + writer over existing `changes`/`returns`/`positions` JSON.
- Curating `data/symbol_overrides.json` for known foreign/ADR CUSIP→EODHD-symbol mappings (improves pricing coverage) is independent.

### Context to load
- `docs/specs/13f_analysis_pipeline_spec.md` (Views 2–4 are §"Layer 2: Analytical Views").
- `docs/implementation_notes/13f_analysis_pipeline_implementation_notes.md` (why things are the way they are).
- `workstreams/main-decisions.md` (2026-06-12 section).
- Build workdir `spec-deviations.md` (SD-1…SD-5) — ephemeral `/tmp` location; consider copying into `docs/` if you want it tracked.
