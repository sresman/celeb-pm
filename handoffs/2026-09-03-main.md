## Handoff -- main -- 2026-09-03

**Session duration**: extended (multi-part; one long session)
**Workstream**: main — analysis layer (`tools/transcripts/`, `analysis/`), Baker/Atreides trigger analysis. The `src/celebpm` pipeline was NOT touched.

### What was built
Branch **`trigger-denominator-ex-spcx`** (pushed to origin; NOT merged to main). `analysis/trigger_analysis.xlsx` grew from 6 trigger sheets to **17 coherent sheets**, all on the ex-IPO-reclass equity denominator.

- `tools/transcripts/build_trigger_workbook.py` — added the two-manager context sheets (Summary, 6 Baker: Holdings/AI Basket/Baskets/Flows/Options/Exits+New, 3 Leo: Holdings/Baskets/Flows), the theme-allocation summary, single-primary basket assignment (`primary_basket()` overlay→reclass bucket→theme→Unassigned), ex-reclass weighting, SPCX/CBRS `[memo]` carve-out, and the **Performance** sheet.
- `tools/transcripts/build_13f_analysis.py` — trigger denominator switched total-book → thesis-investable equity (`ex_reclass_denoms()`, rescale in `annotate_common`, `net_buying_pct` + `narrow()` + new-position 2% on the ex-reclass basis). Regenerates `analysis/13f_signal_triggers_clean.csv` (113 events).
- `tools/transcripts/trigger_forward_returns.py` — NEW. Filing-anchored buy-and-hold forward returns at 1m/1q/6m/1y/2y vs SMH+SPY; writes `analysis/trigger_forward_returns_ex_spcx.{csv,md}`; `compute_stats()` reused by the Performance sheet.
- `tools/transcripts/generate_13f_triggers.py` — docstring only (marked legacy; points at build_13f_analysis).
- NEW data: `analysis/baker_basket_overlay.json` (single primary basket per ticker), `analysis/sa_theme_baskets.json` (Leopold baskets), `analysis/atreides_q2_2026_mtm_vs_trading.csv` (Baker MTM/trading decomposition, parity with the SA CSV).
- `analysis/context_sheets_implementation_notes.md` — full decision log, iterations 1–6.
- Committed Q2-current shared inputs the workbook depends on: `ai_basket_reclassification.json`, `theme_baskets_v3.json`, `filing_to_filing_returns_universal.csv`, `thesis_timeline_v2_flat.json`, `sa_q2_2026_mtm_vs_trading.csv`.

### Decisions made
- **Denominator = thesis-investable equity (COMMON ex `IPO_RECLASS_TICKERS = {SPCX, CBRS}`), not total book.** SpaceX + Cerebras entered the 13F via 2026 IPOs (~$5.36B, not market buys) and were compressing every AI subtheme below the fixed 4%/2% thresholds — worse each quarter as their marks rise. Ex-reclass weights sum to 100%; thresholds behave as calibrated. Reclass names: change_type IPO_RECLASSIFICATION, MTM/Trading blank, excluded from all net-buying/denominators/baskets, shown as `[memo]`.
- **One basket per ticker** on the context sheets (matches the prior single-bucket behavior) via `baker_basket_overlay.json` → reclass bucket → theme; NVDA→Nvidia GPU moat, META/AMZN/GOOGL→Hyperscalers (quasi-index), etc. Full memberships in an `also_in` note, not duplicate rows.
- **Forward returns**: filing-anchored buy-and-hold, EW baskets, 1m/1q/6m/1y/2y vs SMH+SPY; event counts only once its window is fully observed.
- Git forensics (item 6, earlier): `theme_baskets_v3.json`'s uncommitted diff was the two-podcast run (additive keys + 4 new baskets + em-dash re-encode) — no existing ticker membership changed. Verified, not accidental.
- Full reasoning in `workstreams/main-decisions.md` (2026-09-03 section) + `analysis/context_sheets_implementation_notes.md`.

### Current state
- Workbook coherent and correct on the ex-SPCX+ex-CBRS basis, current through the Q2 2026 filing (2026-08-14). `mypy` clean on all three modules. Σ thesis weights = 100% per period; recon gap = 0 on the Baker decomposition.
- **Headline number: ex-reclass net deployment = −$1.35B (net SELLER)** vs $4.00B as-filed; combined reclass carve-out $5.36B (SPCX $4.67B + CBRS $0.69B).
- **Backtest read:** beats SPY at every horizon; ~in line with SMH (largely AI-beta); AI_BASKET_RAMP is the only trigger that beats SMH consistently. Single regime, right-skewed. Forward-returns table is unchanged by the CBRS add (CBRS's only event has no observed forward window yet).
- Two commits pushed (`84036a7` denominator, `c0bd630` 17-sheet rebuild); the CBRS change + these wrap-up docs are in a THIRD commit made this turn. Branch not merged to main.

### Known issues
- `IPO_RECLASS_TICKERS` is duplicated across three tool modules — centralize if a 4th consumer appears.
- The Baker Flows thesis-to-action x-ref can't auto-match SPCX/Orbital (SpaceX theses predate its ticker in the timeline) → that row reads `manual`.
- `analysis/eod_prices/` (price cache) is NOT git-tracked, so the Performance sheet isn't reproducible from a fresh checkout without re-fetching EODHD prices (pre-existing design).
- CROSS_4PCT event count fell 22→18 under the new denominator (higher weights cross earlier and stay above → fewer re-crossings) — expected, but worth eyeballing the specific events if a particular quarter's signal matters.

### Next step
Operator decision: **merge `trigger-denominator-ex-spcx` to main** (open a PR or fast-forward), or request changes first. If merging, decide whether the folded-in two-podcast shared inputs (`ai_basket_reclassification.json`, `theme_baskets_v3.json`, `filing_to_filing_returns_universal.csv`, `thesis_timeline_v2_flat.json`) should instead land via a separate two-podcast commit — they're currently in this branch for reproducibility.

### Parallel work available
- celeb-pm Phase 2 **View 4 (Exit Signals / Survivors)** — still the next `src/celebpm` pipeline build per the top of `workstreams/main.md` (untouched all session).
- The two-podcast corpus workstream (thesis audits/extractions/transcripts/manifests) remains uncommitted — separate from this analysis work.

### Context to load
`analysis/context_sheets_implementation_notes.md` (iterations 1–6 — the full decision log), `workstreams/main-decisions.md` (2026-09-03 section), `tools/transcripts/build_13f_analysis.py` + `build_trigger_workbook.py` + `trigger_forward_returns.py`.
